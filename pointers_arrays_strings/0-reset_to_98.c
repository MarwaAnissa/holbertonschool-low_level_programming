#include <stdio.h>

void reset_to_98(int *n);

int main() {
    
   
int number = 42;

    printf("Before: %d\n", number);
    reset_to_98(&number);
    
    reset_to_98(&number

    reset_to_98

    reset_to
printf("After: %d\n", number);

    return 0;
}
