#include <stdio.h>
 int n = rand() - RAND_MAX / 2 ; printf("%d ", n); if (n > 0) { printf("est positif\n"); } sinon si (n == 0) { printf("est zéro\n"); } else { printf("est négatif\n"); } renvoie 0 ; }
